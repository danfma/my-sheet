# Structured table references, AGGREGATE, and the blocking reference-semantics gaps

Make `Table[Column]` a first-class reference in the MySheet formula engine, implement `AGGREGATE`, and close
the reference-semantics gaps that stop the affected real-world workbooks from evaluating even once the first
two exist. Target version 3.17.0 (minor).

## Provenance and status of this plan

This plan is the output of seven parallel design agents, each followed by an adversarial verifier that re-read
the cited source and PROVED claims by running probes. Every phase came back `needs-revision`: 12 blockers and
19 majors were found and are folded in below as explicit correction items. Nothing here is unreviewed design.

**Do not trust a line number in this document.** They were accurate when written; several of the cited files changed on disk during the analysis itself. Anchor every edit on member names, constant names and heading text, and re-read the file before editing it.

### The governing principle (P0)

Match Excel's (or Aspose.Cells') observable behaviour whenever possible. Never adopt a simpler-but-different
semantics because it is easier to implement. Where MySheet genuinely cannot match Excel — it has no spill
model and no hidden-row model — the deviation must be explicit in the code, justified by the specific missing
capability, and documented for users. For every semantic decision, state which Excel behaviour is being
reproduced. Where Excel's behaviour is unknown, it goes in this plan's open decisions, never into a silent
guess.

**Addendum (user ruling, 2026-09-09): Excel compatibility is ALWAYS the target. Wherever a measured MySheet result differs from Excel's, MySheet's function is what gets adjusted — a divergence is never left as a documented limitation unless MySheet structurally cannot match (no spill model, no hidden-row model). The user's definition (2026-09-09): "when I say Excel, I mean Aspose" — **Aspose.Cells 26.6.0, measured, is THE authority**; the Microsoft documentation page is consulted only for what Aspose cannot be made to answer, and a page never overrides a measurement. This REVERSES Phase 2's "the documented page wins over the oracle" ruling on the nested-AGGREGATE skip (`AGGREGATE(9,0,C1:C3)` must become 8, `AGGREGATE(3,0,C1:C3)` 2) → Phase 11. Consequence: the recorded divergences below are work items (Phases 9, 10 and 11), not open questions.**

### Settled scope

- **S1 — syntax.** `Table[Column]` (spaces, punctuation and Excel's `'`-escaping inside the brackets), plus `Table[#All]`, `Table[#Data]`, `Table[#Headers]`, `Table[#Totals]` and the composite `Table[[#Data],[Column]]`. Explicitly out: `[@Column]`, column spans `[[Col1]:[Col3]]`, implicit-table `[Column]`. Out-of-scope shapes fail with a clear `ParseException` and therefore keep degrading to the cached value plus an `UnparsableFormula` warning on load.
- **S2 — public API.** `Workbook.DefineTable(...)` plus a read-only `Workbook.Tables`, mirroring the `DefinedNames`/`DefineName` pair. Table names are workbook-unique, as in Excel.
- **S3 — interop.** The loader reads `<table>` parts and auto-registers them. `SaveAsExcel` does not write them. `MergeIntoExcel` is unchanged.
- **S4 — cell boundary.** Excel's implicit intersection, in two halves, both verified against Microsoft's "Implicit intersection operator: @" article. For a RANGE: intersect with the formula cell's own row and column — a single-column range spanning the formula's row yields that row's cell, a single-row range spanning its column yields that column's cell, a 1x1 range yields itself, anything else `#VALUE!`. For a computed ARRAY (`FILTER`, `SEQUENCE`, …): the TOP-LEFT value. No `ComputedValueKind.Reference` may escape as a cell value. Consumed inside a function is unchanged. No spill. **Correction measured 2026-09-09 (Phase 10 verifier, Aspose.Cells 26.6.0): for a bare formula in a cell Aspose applies implicit intersection to EACH range operand BEFORE the operator, not to the result — `=E1:E3*10` in L2 = 20, in L3 = 30, in L5 = `#VALUE!`; `=E1:E3*E5:G5` in F2 = 40 (E2×F5); a 2-D operand `=A1:C3*E1:E1` = `#VALUE!`. The "array half" as written (top-left of the computed array) is therefore NOT Aspose's rule; Phase 7 must implement per-operand intersection instead (binding, under the P0 addendum). Phase 1's RANGE half is unaffected.**
- **S5 — dynamic arrays.** `FILTER`/`SORT`/`UNIQUE`/`SEQUENCE` in the consumed position only. Bare in a cell follows S4's array half. The no-spill limitation is documented prominently.
- **S6 — AGGREGATE.** Full `function_num` 1-19 x options 0-7. Documented caveat: MySheet has no hidden-row model, so options 1/3/5/7 behave as 0/2/4/6, consistent with `SUBTOTAL` already mapping 101-111 to 1-11.
- **S7 — versioning.** 3.17.0 minor, with a new one-way compatibility subsection in `docs/serialization.md` covering both the new union tags and the new third `Workbook` member.
- **S8 — blocking gaps.** `ROW`/`COLUMN` must accept any reference-producing expression. `SUMPRODUCT` must accept a computed-array argument.

### What the verification measured that changes the shape of the work

- ~~**`SUBTOTAL` silently returns wrong numbers for array arguments.**~~ **WITHDRAWN 2026-09-09 — the "where Excel gives" half was never measured, and it is wrong.** Only the MySheet side was measured: `SUBTOTAL(9,ROW(A1:A3))` = 1, `SUBTOTAL(9,(A1:A3<>0)*1)` = `#VALUE!`, `SUBTOTAL(2,…)` = 0, `SUBTOTAL(3,…)` = 1. The Excel column (6, 2, 3, 3) was INFERRED by analogy with `SUM`. Measured afterwards on the plan's oracle (Aspose.Cells 26.6.0): every one of those four shapes is `#VALUE!` in Excel, including the constant `SUBTOTAL(9,{1,2,3})` and including CSE entry, because `ref1` really is a reference-only slot. Phase 2 shipped the fold (11f5eaf) on the inference and then reverted it; the range form (`SUBTOTAL(9,A1:A3)` = 14) was correct all along, so the only real defect here was `SUBTOTAL(9,ROW(A1:A3))` answering 1 instead of `#VALUE!`.
- **There are TWO frozen full-`Workbook` wire goldens, not one.** `CellStoreTests.PreChangeCellsWireGolden` and `SheetNameInterningTests.PreInterningWireGolden`. The second was missed by name-based grep because the `0x02` member-count byte appears as base64 `Ag`. Measured: adding the third `Workbook` member fails BOTH.
- **A C# record's `with` copies private fields, so a memoized column index goes stale.** Measured: after warming the memo, `table with { ColumnNames = [...] }` reports a missing column as absent and a reordered column at the wrong index. The memo must live off the record.
- **`RecalculationEngine.EnsureFresh` consumes the staleness signal as a side effect.** Measured: `EstimateImpact` followed by `Recalculate` leaves a stale value behind, because whichever public method runs first refreshes the snapshot. The invalidation must be sticky.
- **`NamedReferences.ValidateName` cannot be reused for table names.** Measured: it rejects `Tabela1` and `Table1` — Excel's own default table names — because `Parser.IsCellReference` accepts any letters-then-digits string with no column/row bound. Reusing it would make the loader unable to register almost every real table. The same fact forces the parser's bracket arm to sit BEFORE the `IsCellReference` check.
- **A version bump alone leaves stale VALUES behind.** Measured on the shipped `DefineName`: after widening a name's range, `SUM(name)` kept its old answer even through `Recalculate` reporting `rebuilt=True`. Only `InvalidateCache` clears layer 3. `DefineTable` would inherit this; the fix repairs `DefineName` too.
- ~~**The array/mini-CSE gap exists for defined names too and is left unfixed.**~~ **CLOSED 2026-09-10 by Phase 11a** (Rule A): `COUNT((Rng<>"")*1)` = 3 now, equal to its literal twin, so tables and defined names will NOT disagree at Phase 5's release. Phase 5 item 10 adds its `TableReference` arm beside the `NameReference` arm in the same two switches. **The `IsBareReferenceNode` half of this sentence was wrong and is corrected here:** the predicate reads `expression is Reference or NameReference` and its own doc already says "a name today, a structured table reference tomorrow", so a node deriving from `Reference` needs NO change there — and `SUM(Tabela1[Valor])` therefore takes the reference path at a consumer's top level for free. What the two switches need is Phase 11a's shared FOUR-outcome oracle shape, not the one-outcome arm the older corrections prescribe.

## For Future Agents

As work proceeds: mark checkboxes `- [x]` as items complete; when a phase is done, set its status to
`Complete` and write its **Phase Summary** (what was done, key decisions, anything needed to continue with
zero context); run the phase's **Verification Plan** and record the result before moving on. When all phases
are done, fill in **Final Recap** and **Deployment Plan**.

Rules specific to this repo, which override habit:

- **TDD is not optional here.** Write the failing test, run it, confirm it fails for the stated reason, then implement. Several items below exist only because a verifier ran the design and watched it fail.
- **A fixture value that coincides with the correct answer is an assertion that cannot fail.** When a test distinguishes "read from cache" from "computed", the cached value must be a deliberate lie.
- Tests run with `dotnet run --project tests/<proj>/<proj>.csproj`, NOT `dotnet test` (TUnit on Microsoft.Testing.Platform). Filter with `-- --treenode-filter "/*/*/<Class>/*"`.
- Gates before any commit: `dotnet csharpier format .` then `check .`, `dotnet build Danfma.MySheet.slnx -c Release` with zero warnings, and both suites green. Baseline measured for this plan: **1203** core + **88** Excel. Re-measured on main 2026-09-10 after Phases 1, 2, 3, 8, 9, 10, 11a and 11b: **1817** core + **93** Excel, and **1921** core + **93** Excel on the Phase 7 branch, whose core count includes ONE deliberate red pin. Use the current numbers, not this line's original ones.
- `CHANGELOG.md` is generated by versionize — never hand-write it. Use Conventional Commits; scopes in use are `parser`, `eval`, `serialization`, `recalc`, `dirty-graph`, `excel`.
- **Union tags must be assigned in ONE coordinated edit.** Two phases append `[MemoryPackUnion]` tags. **The next free tag is 327**, not the 322 this line used to give: 322 went to `Aggregate` (Phase 2) and 323, 324, 325 and 326 to Phase 7's FILTER, SORT, UNIQUE and SEQUENCE. If two phases each write the same number, MemoryPack fails at type INITIALIZATION with a duplicate-tag error, not at compile time. Re-count `Expression.cs` before trusting any number here, and count with `^\[MemoryPackUnion` — a bare `grep -c MemoryPackUnion` is one too high because the append-only policy comment contains the word.
- `docs/pt-BR/` is a full mirror (11 files, identical names). Every doc edit needs its twin. `README.md` has no mirror.
- Golden-value tests cite the Microsoft docs page plus its GUID and the fetch date. Fetch it at implementation time; do not invent a GUID.

## Phases

Each phase lives in its own file so a future agent loads only the one it is working on. Order below is a
topological sort of the design dependencies, then earliest user value. Phases 1 and 2 have no dependencies and
each delivers something verifiable on its own; the corpus formulas need Phase 1 even after Phases 3-5 land,
which is why it goes first.

| # | Phase | File | Items | Blockers | Majors | Status |
| --: | --- | --- | --: | --: | --: | --- |
| 1 | ROW/COLUMN over any reference, Excel's implicit intersection, array-native SUMPRODUCT | [`phase-1-reference-semantics.md`](structured-table-references-and-aggregate/phase-1-reference-semantics.md) | 27 | 2 | 2 | **Complete** — `feat/reference-semantics`, `32a67f6..c04e78b`, merged ff into main (verified ancestor) |
| 2 | AGGREGATE(function_num, options, ref1, [k]) on a shared SUBTOTAL/order-statistics core | [`phase-2-aggregate.md`](structured-table-references-and-aggregate/phase-2-aggregate.md) | 18 | 1 | 1 | **Complete** — `feat/aggregate`, `a6aba56..3d030ae`, merged ff into main |
| 3 | Table model, Workbook.DefineTable/Tables, and the third serialized Workbook member | [`phase-3-table-model-registry.md`](structured-table-references-and-aggregate/phase-3-table-model-registry.md) | 26 | 3 | 2 | **Complete** — `feat/table-model`, merged ff; core 1777, Excel 93; the only phase that changes the persistence format, with both compatibility halves measured |
| 4 | Bracket lexing and the structured-reference grammar | [`phase-4-lexer-parser.md`](structured-table-references-and-aggregate/phase-4-lexer-parser.md) | 18 | 2 | 1 | Not started |
| 5 | Structured-reference resolution and cross-cutting graph integration | [`phase-5-resolution-and-graph.md`](structured-table-references-and-aggregate/phase-5-resolution-and-graph.md) | 24 | 1 | 5 | Not started |
| 6 | The .xlsx loader reads <table> parts into Workbook.Tables | [`phase-6-excel-loader.md`](structured-table-references-and-aggregate/phase-6-excel-loader.md) | 25 | 1 | 4 | Not started |
| 7 | FILTER / SORT / UNIQUE / SEQUENCE as mini-CSE producers | [`phase-7-dynamic-arrays.md`](structured-table-references-and-aggregate/phase-7-dynamic-arrays.md) | 21 | 2 | 4 | **Complete** — `feat/dynamic-arrays`, 21 commits off main, head `e6f14c7`; core **1927 / 1**, Excel 93 / 0. The one failure is deliberate: `ALetBoundProducer_InACriteriaSlot` (renamed `ALetBoundProducer_StreamsWhole_AndIsRefusedInACriteriaSlot` once the limit closed) is red on all three rows because a LET binding escapes every array gate, and a green pin at today's value would hide the first formula a user of this phase writes — **closed by Phase 11c**. Eight tasks, the eighth unplanned — a producer under a scalar-condition `IF` collapsed to its top-left, a silent wrong number the phase itself introduced. Two page-over-oracle rulings (`exactly_once`, `AVERAGE(UNIQUE)`), three measured behaviours that contradicted the design and won (blanks preserved, errors sorted, UNIQUE case-sensitive), and the design's fourth deviation (blank-to-zero) deliberately NOT shipped. Union tags 323-326; `#CALC!` is the eighth error code. The design of record is [`plans/dynamic-array-functions.md`](dynamic-array-functions.md).
The three-part final review found TWO real defects in the phase's own closing commit, both silent and both mine: a volatile condition could still collapse a producer under a scalar-condition `IF` (100 of 400 seeded workbooks answered 1 for `SUM(IF(RAND()<0.5,A1:A3,SEQUENCE(3)))`), and the branch probe broke its lockstep with the build for an open range under an array condition, drawing a volatile twice. It also found the `exactly_once` mechanism wrong in eight places, a producer mis-flagged `Elementwise` recursing to a stack overflow rather than failing a test, two more "pinned" claims for rows with no assertion, and five commits of mine carrying AI attribution trailers against the user's standing rule — rewritten with byte-identical trees. Copilot returned zero findings and its `exactly_once` table was partly fabricated, the fourth phase in a row |
| 8 | Elementwise lifting of unary operators and scalar functions in the mini-CSE — **executes after Phase 2, before Phase 7** (added 2026-09-09 for the user's "Part 3" report: 5 of its 7 formulas already pass after Phase 1; the rest are engine-wide mini-CSE eligibility gaps) | [`phase-8-elementwise-lifting.md`](structured-table-references-and-aggregate/phase-8-elementwise-lifting.md) | 14 | 1 | 3 | **Complete** — `feat/elementwise-lifting`, `3d030ae..dc29d30`, merged ff; core 1490, Excel 90; guard pins the 180 by name after the final review found a contributor-style mutation shipping green |
| 9 | Excel's date serial epoch (serial 1 = 1900-01-01, phantom 1900-02-29 at 60) — **executes after Phase 8** (added 2026-09-09 under the P0 addendum) | [`phase-9-date-epoch.md`](structured-table-references-and-aggregate/phase-9-date-epoch.md) | — | — | — | **Complete** — `feat/date-epoch`, `f0fbb4a..275c25e`, merged ff; core 1537, Excel 93; the user's 1900 working-day exception is implemented and bounded, with 76 modern pins and zero deviation at serial >= 61 |
| 10 | Vector broadcasting in the mini-CSE (1xN / Nx1 against MxN, Excel's rule) — **executes after Phase 9** (added 2026-09-09) | [`phase-10-vector-broadcasting.md`](structured-table-references-and-aggregate/phase-10-vector-broadcasting.md) | — | — | — | **Complete** — `feat/vector-broadcasting`, `897affc..HEAD`, merged ff; core 1582, Excel 93; twelve expectations flipped, 0 B/element held, four measured deviations pinned |
| 11a | Unblocking slice of the sweep — the defined-name mini-CSE gap (item 8 + correction B2) and the criteria family's silent answers over a computed argument (item 22) — **executed after Phase 10 and before Phase 7** (added 2026-09-10; the original note also said "before Phase 3", which Phase 3's completion has made unsatisfiable — 11a in fact landed after it) | [`phase-11a-unblocking-slice.md`](structured-table-references-and-aggregate/phase-11a-unblocking-slice.md) | 9 | — | — | **Complete** — `feat/unblocking-slice`, `4c083da..HEAD`; core 1634 / 0, Excel 93 / 0; a name in an array position is now what it is bound to, a computed array in a criteria range slot is `#REF!`, four expectations flipped, four measured divergences handed to Phase 11 — the LET-bound-computed-array half of the standing `LET` limit among them, **closed by Phase 11c** |
| 11b | Boolean text coercion — `IF`, `NOT` and `IFS` accept the text `TRUE`/`FALSE` in a condition slot (case-insensitive, no trimming), the opt-in `CoerceToBoolAllowingTextWords` beside the shared helper, `AND`/`OR`/`XOR` untouched by measurement — **executed after Phase 11a, in parallel with Phase 7** (added 2026-09-10 from a user bug report) | [`phase-11b-boolean-text-coercion.md`](structured-table-references-and-aggregate/phase-11b-boolean-text-coercion.md) | — | — | — | **Complete** — `feat/boolean-text`, `90df14b..b86e5bc`, merged ff; core 1676 / 0, Excel 93 / 0; a pure addition of 37 lines called from four sites, so the nine other callers of the shared helper are provably untouched |
| 11c | Array bindings — `LET`, `CHOOSE`, unary `+` and a defined name stop collapsing a computed array to its top-left, and the criteria gate sees through them — **executes after Phase 7, before Phases 4, 5 and 6** (added 2026-09-10; closes Phase 7's one deliberately red pin and Phase 11a's standing LET limit; ships in the same 3.20.0 release as Phase 7) | [`phase-11c-array-bindings.md`](structured-table-references-and-aggregate/phase-11c-array-bindings.md) | 15 | — | — | **Complete** — `feat/array-bindings`, `6ad7cea..ceb7539`, engine head `ceb7539`; core **2001 / 0** — zero failures for the first time since Phase 7 registered its producers — Excel **93 / 0**; `LET`'s bound name, `CHOOSE`'s chosen branch, unary `+`'s operand and a defined name's definition each carry a computed array to their consumer through one `ArrayBindings.Capture` helper, built once, with the one context-aware `IsBareReferenceNode(Expression, EvaluationContext)` predicate telling the criteria gate an array binding is not a reference; no new `ComputedValueKind` |
| 11 | Excel-compatibility sweep of the recorded divergences (nested-AGGREGATE skip → count as Aspose does, scalar in a `ref` slot, `MODE.SNGL` tie-break, single error cell under option 6, defined-name mini-CSE gap, `SUBTOTAL(9,IF(TRUE,A1:A3,B1:B3))`) — **executes after Phase 10** (added 2026-09-09; the original note said "before Phase 3", which Phase 3's completion has made unsatisfiable — the sweep now runs after the table chain, per the user's ordering) | [`phase-11-excel-compatibility-sweep.md`](structured-table-references-and-aggregate/phase-11-excel-compatibility-sweep.md) | — | — | — | Not started — design verified 2026-09-09 (2 blockers, 7 majors, 14 minors folded in) and grown to 20 items, 8 of them measured during Phases 8 and 9 |

Keep this table's Status column in step with each phase file's own `Status:` line — it is the first thing a
resuming agent reads.

## Integration and release cadence (decided 2026-09-09)

Each phase, on completion: a three-part final review — a Fable-model subagent, `zclaude --model opus` (GLM-5.3) and `copilot` — consolidated and fixed, then `git rebase main` + `git merge --ff-only` into `main` (no merge commits; the user's rule). The 3.17.0 release is prepared only after ALL phases (1-8) are merged: `versionize` dry-run to preview the CHANGELOG and bump, the serialization compatibility notes verified against the final union-tag list and the Workbook member count, then the manual Release workflow.

## Open decisions that need a human, not more analysis

These block no phase from starting, but each one changes a shipped behaviour or an advertised guarantee. They
are collected here so they are answered deliberately rather than by omission.

- **Should the array/mini-CSE gap be fixed for defined names too, or shipped inconsistent?** It is a measured, pre-existing silent-wrong-answer bug (`COUNT((Rng<>"")*1)` = 1, truth 3). Fixing it means threading an `EvaluationContext` overload through `ArrayEvaluation.IsArrayEligible`/`Probe` and deciding scalar-vs-range at four call sites — roughly the size of three items again. Fixing it makes tables and names consistent; deferring ships a documented inconsistency. **DECIDED 2026-09-09: fix it (Excel gives 3) → Phase 11.** **DELIVERED 2026-09-10 in Phase 11a** (Rule A): `COUNT((Rng<>"")*1)` = 3, `COUNT(Rng*1)` = 3, `INDEX(Rng*2,3)` = 18, `SUM(LEN(MyName))` = 6 — a name in an array position is whatever it is bound to, while a bare name at a consumer's top level keeps the reference path. Three shapes are left over and named in that file: an open-range name (cost guard), a union name (the union ruling owns it) and a `LET` node in a consumer's own argument slot (Phase 7's `LET` routing).
- **Should `DefineName` start rejecting a name a table already uses?** It is Excel's rule and keeps one invariant, but it adds a failure mode to a shipped API, and in MySheet the two syntaxes are disjoint so the collision is harmless to resolution. Alternative: enforce uniqueness only in `DefineTable` and document the asymmetry.
- **Should a follow-up teach `SaveAsExcel` to emit the `<table>` part?** S3 says no. The measured cost of no: load a table workbook, export in `FormulaMode.Formulas`, and Excel opens the result showing `#NAME?` because the formula now parses but the part is absent. Today that is impossible because the formula never parsed. `ExcelExportOptions` has no warning channel, so documentation is the only mitigation inside S3.
- **Should `MergeIntoExcel` stop writing a degraded cell's stale cached number?** Already shipped and documented as a limitation, but under P0 the Excel-faithful answer is arguably to leave the target's formula alone so Excel recomputes, rather than freezing a number no engine derived from current inputs.
- ~~**Should `ROWS`/`COLUMNS`/`AREAS` be brought in line with `ROW`'s new error propagation?**~~ **DECIDED 2026-09-08: yes, in Phase 1, under P0.** `ROWS(NoSuchName)` returns 1 today where Excel gives `#NAME?`. It is a behaviour change to four functions and ships as `feat(eval):`, not `fix`. See Phase 1's "Scope addition" section.
- **Date serial epoch: MySheet's serial 1 is 1899-12-31, Excel's is 1900-01-01** (found by Phase 8's verifier: `MONTH(1)` = 12 here, 1 in Excel). Pre-existing P0 divergence outside every phase's scope; needs its own decision (and a migration note if changed). **DECIDED 2026-09-09 under the P0 addendum: adopt Excel's epoch (serial 1 = 1900-01-01, including Excel's phantom 1900-02-29 at serial 60) → Phase 9.**
- **`MODE.SNGL` tie-break** (found by Phase 2 Task 2): MySheet returns the first value to REACH the winning count, not the first encountered (`MODE.SNGL({2;1;1;2})` = 1); likely diverges from Excel; now a one-line change in `StatisticsMath.Mode`. Needs an Excel oracle. **DECIDED 2026-09-09: match Excel (measure on the oracle first) → Phase 11.**
- **Should a direct SCALAR in a `ref` slot be rejected?** MySheet accumulates it: `SUBTOTAL(9,7)` = 7, `SUBTOTAL(9,A1:A3,7)` = 21, `AGGREGATE(9,4,A1:A3,7)` = 21, `AGGREGATE(9,4,7)` = 7. Aspose.Cells 26.6.0 answers `#VALUE!` to every one of them (measured 2026-09-09): `ref1, ref2, …` are reference-only slots, the same rule the computed-array reversal now enforces for arrays. It is *shipped* SUBTOTAL behaviour, and this plan already lists the referenced-vs-direct scalar semantics as a deliberate deviation, so the Phase 2 fix wave left it alone. The array slot is genuinely different and MySheet already matches there: `AGGREGATE(15,6,7,1)` = 7 on both sides. Cost of fixing: one arm in `AggregateCodes.Gather`'s `default:`, against whatever real workbooks pass a literal where a ref is declared. **DECIDED 2026-09-09: yes, `#VALUE!` as the oracle answers → Phase 11.**
- **What should a single error CELL answer in the array form's slot under option 6?** MySheet: `AGGREGATE(15,6,E2,1)` and `AGGREGATE(15,6,E2:E2,1)` are `#NUM!` — the error is skipped, the population is empty, `SMALL` fails on it. Aspose answers `#DIV/0!` to both, yet ignores the error once the range is wider (`AGGREGATE(15,6,E1:E3,2)` = 9, matching MySheet) and answers `#NUM!` when every cell is an error (also matching). Its rule looks like "a 1x1 reference collapses to a scalar before the option applies", which no documentation states. Same family: after the whole-argument-error fix, `AGGREGATE(15,6,1/0,1)` is `#DIV/0!` here and `#VALUE!` there, while `AGGREGATE(15,6,7,1)` is 7 on both — the oracle rejects only an *error* scalar in the array slot. Whichever answer is chosen should cover all three shapes at once; none of them was changed in the fix wave. **DECIDED 2026-09-09: whatever the oracle answers; measure, then match → Phase 11.**
- **Should bare `=SEQUENCE(5)` really return 1?** That is Excel's verified `@`-on-array behaviour and what Excel writes when upgrading a legacy formula, but it is not what modern Excel does with the un-prefixed formula. A user who types `=FILTER(A:A,B:B>0)` and sees one value will read it as a bug. `#VALUE!` is the defensible alternative.

### Questions settleable without a human, by experiment

Several phases need an Excel oracle for a semantic this plan could not verify (what `[#Totals]` returns when
the table has no totals row; whether implicit intersection of a 2-D range uses both axes; how SORT orders
mixed types). Neither machine in this session has Excel or LibreOffice. Two oracles are available and should
be used before the affected arm is coded. Phase 2 ran the second one and it settled every question it was
pointed at — including one the phase had already answered WRONG by inference (see that phase's "SETTLED"
section); use it BEFORE coding an arm, not after shipping it:

- **The self-verifying fixture.** Author a `.xlsx` in real Excel containing the formula in question, then read the cached `<v>`/`t="e"` Excel stored next to it — the loader already reads that cache, so the fixture answers the question by itself and becomes a regression test.
- **Aspose.Cells 26.6.0**, already referenced by `benchmarks/Danfma.MySheet.Benchmark`. It resolves every formula in the original report correctly, so it can serve as the oracle for anything Excel is not on hand for.
- **Vector broadcasting in the mini-CSE (found by Phase 8 Task 1, 2026-09-09).** Excel and Aspose.Cells 26.6.0 broadcast a 3x1 vector across a 3x3 rectangle: `SUM(A1:C3*E1:E3)` = 108, `SUM(ROUND(A1:C3,E1:E3))` = 45, `SUM(LEFT(D7:F9,E6:E8))` = 0 on the oracle. MySheet's `BinaryOperand` requires equal shapes (scalar broadcast only) and answers `#VALUE!` per element — pre-existing, independent of Phase 8, and pinned as-is by `ElementwiseLiftingTests` with the divergence stated. Fix belongs to a later phase (extend the operand shape rule to 1xN / Nx1 against MxN, Excel's rule); Phase 8 must not change it by accident. **DECIDED 2026-09-09: match Excel's vector broadcasting → Phase 10.**
- ~~**Should the working-day functions reproduce Aspose's 1900 composite, and should unions in a `ref` slot follow Aspose?**~~ **DECIDED 2026-09-09 by the user, both as exceptions to the Aspose rule.** Aspose contradicts itself in both places (no derivable WORKDAY/NETWORKDAYS rule below serial 61, best of 14 candidates wrong on 17/580 rows and NETWORKDAYS non-additive; a union is a flattened array for `ISREF`/`ROWS` yet a reference for `ROWS(UnionName)`), so P0's "genuinely cannot match" clause applies: MySheet walks the real calendar below 61 and keeps its union support, both pinned as deliberate divergences with the measurements quoted. `MODE`'s row-major scan order is scoped to the two MODE feeders. See Phase 9's and Phase 11's USER RULING sections.


## Final Recap

_(write when all phases complete)_

## Deployment Plan

_(write when all phases complete)_
