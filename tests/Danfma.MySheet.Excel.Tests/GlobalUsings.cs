// Global using directives

// Only the categories these tests actually consume: the OpenXML SDK also declares Row/Column/Text,
// so importing every Danfma.MySheet.Expressions.* category here would create CS0104 ambiguities.
global using Danfma.MySheet.Expressions.Mathematics;
